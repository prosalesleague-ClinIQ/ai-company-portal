node_modules
.next
.vercel
out
dist
.env*
!.env.example
*.tsbuildinfo
next-env.d.ts
.DS_Store
# Manifest is committed so Vercel can build without filesystem access
# Rebuild locally with `npm run rebuild` then commit.
# public/manifest.json
